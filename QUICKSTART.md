# 🚀 Быстрый старт AI Email Assistant

Привет! Вот пошаговая инструкция для начала работы с проектом.

## ⚡ 5 минут до первого запуска

### 1. Откройте терминал и перейдите в папку проекта

```bash
cd "d:\Projects\Flutter\AI Email Assistant\AI-Email-Assistant"
```

### 2. Убедитесь, что зависимости установлены

```bash
npm install
```

### 3. Запустите проект

```bash
npm start
```

### 4. Выберите, где запустить

После выполнения `npm start` вы увидите меню:

- **`a`** - запустить на Android эмуляторе
- **`i`** - запустить на iOS симуляторе (только macOS)
- **`w`** - запустить в веб-браузере
- **`j`** - запустить в Expo Go на телефоне (отсканировать QR код)

## 📱 Установка Expo Go для тестирования на телефоне

1. Установите приложение **Expo Go** из App Store или Google Play
2. Отсканируйте QR код, который появится в терминале
3. Приложение загрузится и откроется на вашем телефоне

## 📂 Основные файлы, на которые стоит обратить внимание

- **`App.tsx`** - точка входа приложения
- **`src/screens/HomeScreen.tsx`** - главный экран (начните отсюда!)
- **`src/context/ThemeContext.tsx`** - управление темой
- **`src/components/`** - переиспользуемые компоненты
- **`src/services/`** - API и бизнес-логика

## 🎨 Как изменить дизайн

### Изменить основной цвет

Откройте `src/utils/themes.ts` и измените:

```typescript
// Строка ~6 и ~19
primary: '#0066FF',  // Измените на нужный цвет
accent: '#0066FF',   // И здесь
```

### Изменить текст на главной странице

Откройте `src/screens/HomeScreen.tsx` и найдите `HeroSection`:

```tsx
<HeroSection
  title="Ваш новый текст здесь"
  subtitle="Ваше описание здесь"
>
```

## 🔧 Работа с компонентами

Все компоненты находятся в `src/components/`:

1. **Button** - кнопка с разными стилями
2. **Card** - карточка с тенью
3. **HeroSection** - заголовок с описанием
4. **FeatureItem** - элемент функции (с иконкой)
5. **StepItem** - шаг инструкции
6. **DemoModal** - модальное окно

Смотрите примеры в файле `EXAMPLES.md`

## 🔌 Интеграция с Supabase

1. Создайте аккаунт на [supabase.com](https://supabase.com)
2. Создайте новый проект
3. Скопируйте URL и Anon Key
4. Откройте файл `.env` и добавьте:

```env
EXPO_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your-key-here
```

## 📚 Где искать помощь

- **DEVELOPMENT.md** - подробное руководство разработки
- **EXAMPLES.md** - примеры кода для всех функций
- **README.md** - основная документация проекта
- **Код компонентов** - все хорошо задокументировано с комментариями

## 🐛 Если что-то не работает

### Ошибка "Module not found"

```bash
# Удалите node_modules и переустановите
rm -rf node_modules
npm install
```

### Старый кэш Expo

```bash
# Очистите кэш
expo start -c
```

### Проблемы на Android/iOS

```bash
# Очистите build файлы
npm start -- --clear
```

## ✨ Что дальше?

1. **Исследуйте код** - покопайтесь в компонентах
2. **Создайте свой компонент** - попробуйте создать новый компонент в `src/components/`
3. **Добавьте экран** - создайте новый экран в `src/screens/`
4. **Подключите API** - интегрируйте с реальным API в `src/services/`

## 🎯 Цель приложения

Приложение помогает пользователям писать письма быстрее с помощью AI:

- ✍️ Исправляет ошибки
- 🎭 Меняет тон письма
- 💬 Генерирует ответы
- 📧 Интегрируется с Gmail (в будущем)

## 📞 Контакты

Если у вас есть вопросы - смотрите DEVELOPMENT.md или добавляйте свои комментарии в коде!

---

**Готовы начать? Введите `npm start` в терминале и создавайте! 🚀**
