# 📧 Gmail Integration - Краткое руководство

## ✨ Что было добавлено

### 🆕 Новые файлы

| Файл | Описание |
|------|---------|
| `src/services/gmailService.ts` | Сервис для работы с Gmail API через OAuth |
| `src/screens/GmailAuthScreen.tsx` | Экран авторизации через Google |
| `GMAIL_SUPABASE_SETUP.md` | Подробная инструкция по настройке Supabase |

### 📝 Обновленные файлы

| Файл | Изменения |
|------|-----------|
| `src/navigation/RootNavigator.tsx` | Добавлена навигация на GmailAuthScreen |
| `src/screens/HomeScreen.tsx` | Обработка параметров навигации, кнопка теперь ведет на GmailAuth |
| `src/screens/index.ts` | Экспорт новой GmailAuthScreen |
| `src/types/index.ts` | Добавлены параметры навигации |

## 🚀 Быстрый старт

### Шаг 1️⃣: Настроить Supabase

1. Откройте [Supabase SQL Editor](https://supabase.com/)
2. Скопируйте и выполните SQL из **GMAIL_SUPABASE_SETUP.md**
3. Это создаст необходимые таблицы и политики RLS

### Шаг 2️⃣: Получить Google Client ID

1. Перейдите на https://console.cloud.google.com
2. Создайте новый проект
3. Включите **Gmail API**
4. Создайте **OAuth 2.0 Client ID** (Web application)
5. Добавьте Redirect URI: `com.aiemailassistant:/oauth2redirect`
6. Скопируйте **Client ID**

### Шаг 3️⃣: Добавить Client ID в .env

```env
EXPO_PUBLIC_GMAIL_CLIENT_ID=your_client_id.apps.googleusercontent.com
```

### Шаг 4️⃣: Тестировать

```bash
npm start
# Откройте приложение
# Нажмите "Подключить Gmail"
# Авторизуйтесь через Google
```

## 📱 Экран авторизации

### Элементы интерфейса

```
┌────────────────────────────────────┐
│ ← Назад                            │
├────────────────────────────────────┤
│                                    │
│            📧 (большой)            │
│      Подключите Gmail              │
│                                    │
│ Приложению нужен доступ к вашим   │
│ письмам, чтобы помогать с ответ.. │
│                                    │
│ 🔒 Безопасность                    │
│    Ваши письма остаются в Google   │
│                                    │
│ ⚡ Быстро                          │
│    Мгновенная обработка ответов    │
│                                    │
│ 🎯 Умно                            │
│    AI понимает контекст писем      │
│                                    │
│ ┌──────────────────────────────┐   │
│ │ Требуемые разрешения:        │   │
│ │ • Чтение писем               │   │
│ │ • Создание писем             │   │
│ │ • Чтение профиля             │   │
│ └──────────────────────────────┘   │
│                                    │
│   [G Войти через Google]           │
│                                    │
│ Вы можете отключить доступ в     │
│ любое время в настройках Google    │
│                                    │
└────────────────────────────────────┘
```

## 🔄 Поток авторизации

```
1. Пользователь нажимает "Подключить Gmail"
   ↓
2. Открывается GmailAuthScreen
   ↓
3. Пользователь нажимает "Войти через Google"
   ↓
4. Открывается WebView с Google авторизацией
   ↓
5. Пользователь вводит email и пароль
   ↓
6. Пользователь подтверждает разрешения
   ↓
7. Код авторизации отправляется приложению
   ↓
8. Код обменивается на access_token и refresh_token
   ↓
9. Токены сохраняются в Supabase
   ↓
10. Профиль пользователя обновляется (gmail_connected = true)
   ↓
11. Пользователь видит сообщение "✅ Gmail успешно подключен!"
   ↓
12. Возврат на главный экран
```

## 🔐 GmailService - Методы

### `authenticate()`
Запускает OAuth поток и возвращает токены

```typescript
const { accessToken, refreshToken } = await gmailService.authenticate();
```

### `saveTokens(userId, accessToken, refreshToken)`
Сохраняет токены в Supabase и Secure Store

```typescript
await gmailService.saveTokens(userId, accessToken, refreshToken);
```

### `getProfile(accessToken)`
Получает информацию о профиле Gmail

```typescript
const profile = await gmailService.getProfile(accessToken);
// { id, emailAddress, historyId }
```

### `listMessages(accessToken, maxResults)`
Получает список писем из Gmail

```typescript
const messages = await gmailService.listMessages(accessToken, 10);
// [{ id, threadId, labelIds, snippet }, ...]
```

### `getSavedToken(userId)`
Получает сохраненный токен из Secure Store

```typescript
const token = await gmailService.getSavedToken(userId);
```

### `isGmailConnected(userId)`
Проверяет, подключен ли Gmail к аккаунту

```typescript
const connected = await gmailService.isGmailConnected(userId);
```

### `disconnectGmail(userId)`
Отключает Gmail и удаляет токены

```typescript
await gmailService.disconnectGmail(userId);
```

## 📊 Структура данных

### GmailProfile
```typescript
interface GmailProfile {
  id: string;              // Gmail ID пользователя
  emailAddress: string;    // Email адрес
  historyId: string;       // History ID для синхронизации
}
```

### GmailMessage
```typescript
interface GmailMessage {
  id: string;              // Message ID
  threadId: string;        // Thread ID
  labelIds: string[];      // Labels (INBOX, SENT, etc)
  snippet: string;         // Preview текста письма
}
```

### GoogleTokenResponse
```typescript
interface GoogleTokenResponse {
  access_token: string;     // Токен доступа к Gmail
  refresh_token?: string;   // Токен для обновления (один раз)
  expires_in: number;       // Время жизни в секундах
  token_type: string;       // Обычно "Bearer"
  scope: string;            // Запросенные разрешения
}
```

## 🛡️ Безопасность

### Что хранится где

| Данные | Место хранения | Защита |
|--------|---|---|
| Access Token | Supabase + Secure Store | Encrypted, RLS |
| Refresh Token | Supabase только | Encrypted, RLS |
| OAuth Scope | В переменной | Константа в коде |
| Client Secret | - | НЕ ИСПОЛЬЗУЕТСЯ (публичное приложение) |

### Разрешения Gmail API

Приложение запрашивает только одно разрешение:
```
https://www.googleapis.com/auth/gmail.modify
```

Это разрешение позволяет:
- ✅ Читать письма
- ✅ Отправлять письма
- ✅ Изменять письма
- ❌ Удалять письма (безопасно)
- ❌ Доступ к другим гугл сервисам

## ⚠️ Ограничения и известные проблемы

### Локальная разработка

- **CORS**: WebBrowser может показать CORS ошибку в браузере, это нормально
- **Redirect URI**: Убедитесь, что она добавлена в Google Cloud Console

### Production

- Используйте HTTPS для всех коммуникаций
- Храните Client Secret на сервере (если используете)
- Регулярно проверяйте и ротируйте токены

## 🧪 Тестирование

### Проверочный список

- [ ] Суперба таблицы созданы
- [ ] Google Client ID добавлен в .env
- [ ] Приложение запускается без ошибок
- [ ] Кнопка "Подключить Gmail" видна на главном экране
- [ ] Нажатие на кнопку открывает GmailAuthScreen
- [ ] GmailAuthScreen отображается корректно
- [ ] Кнопка "Войти через Google" открывает WebView
- [ ] WebView показывает Google авторизацию
- [ ] После авторизации появляется сообщение об успехе
- [ ] Токены сохраняются в Supabase
- [ ] Профиль пользователя обновляется

## 📞 Отладка

### Проверить консоль

```bash
npm start
# Откройте консоль (Ctrl+M в Expo Go)
# Посмотрите на console.log и console.error
```

### Проверить Supabase

1. Откройте Supabase Dashboard
2. Перейдите в таблицу `user_tokens`
3. Проверьте, есть ли записи после авторизации

### Проверить логи Network

1. В Expo Go нажмите "Enable Remote JS Debugging"
2. Откройте DevTools браузера (F12)
3. Перейдите на вкладку Network
4. Посмотрите на запросы к `oauth2.googleapis.com`

## 🚀 Следующие шаги

После успешной авторизации Gmail:

1. Получение писем из почты
2. Анализ писем через DeepSeek
3. Генерация ответов
4. Отправка черновиков на почту
5. Синхронизация в реальном времени

---

**Версия**: 1.0.0  
**Дата**: 19 февраля 2026  
**Статус**: ✅ ГОТОВО
