# InboxScreen - Документация

## Обзор

Создан полнофункциональный экран `InboxScreen` для отображения списка писем из Gmail API.

## Структура компонентов

### 1. InboxScreen.tsx
Главный экран входящих писем с полной функциональностью.

**Основной функционал:**
- ✅ Загрузка писем из Gmail API (максимум 25)
- ✅ Отображение в виде списка карточек
- ✅ Pull-to-refresh для обновления
- ✅ Поиск по письмам (subject, sender, snippet)
- ✅ Фильтры:
  - Все письма
  - Срочные (AI определенные)
  - Непрочитанные
- ✅ AI-сортировка всех писем
- ✅ Пустое состояние для разных случаев
- ✅ Проверка подключения Gmail

**State переменные:**
```typescript
const [messages, setMessages] = useState<MessageForDisplay[]>([])
const [filteredMessages, setFilteredMessages] = useState<MessageForDisplay[]>([])
const [loading, setLoading] = useState(false)
const [refreshing, setRefreshing] = useState(false)
const [searchQuery, setSearchQuery] = useState('')
const [filterMode, setFilterMode] = useState<'all' | 'important' | 'unread'>('all')
const [aiSortingInProgress, setAiSortingInProgress] = useState(false)
const [accessToken, setAccessToken] = useState<string | null>(null)
```

**Ключевые функции:**
- `loadAccessToken()` - Загрузка токена Gmail из Supabase
- `loadMessages()` - Загрузка писем через gmailService
- `applyFilters()` - Применение фильтров и поиска
- `handleAISorting()` - AI анализ писем на важность
- `handleMessagePress()` - Навигация на экран письма

### 2. MessageCard.tsx
Компонент карточки письма для отображения в списке.

**Отображаемые данные:**
- Avatar с первой буквой отправителя
- Имя и email отправителя
- Тема письма
- Первые 2 строки текста
- Время получения (форматированное)
- Иконки:
  - ⭐ для помеченных писем
  - 🔴 для срочных
  - 🤖 для писем с высокой AI оценкой
- Badge "Срочное" для важных писем
- Синяя полоса слева для непрочитанных

**Стили:**
- Unread состояние: жирный текст, синяя полоса
- Selected состояние: серый фон
- Hover эффекты через Pressable

### 3. MessageDetailScreen.tsx
Экран детального просмотра письма (заглушка с полной структурой).

**Функции:**
- Просмотр полного письма
- Управление звездой (save/unsave)
- Кнопка ответа (заглушка для версии 2)
- Отправка в спам
- Отображение AI оценки важности
- Информация об отправителе
- Статусы письма (Непрочитано, Срочное)

## gmailService методы

### getMessages(accessToken, maxResults = 20)
```typescript
// Возвращает массив MessageForDisplay
const messages = await gmailService.getMessages(accessToken, 25)
```

**Процесс:**
1. Получает список ID писем через Gmail API
2. Параллельно загружает полные данные каждого письма
3. Парсит headers (From, Subject, Date)
4. Извлекает snippet (предпросмотр)
5. Определяет статусы (Read, Starred, Important)
6. Возвращает отсортированный массив

### getMessage(accessToken, messageId)
```typescript
// Получает полное письмо по ID
const message = await gmailService.getMessage(accessToken, messageId)
```

**Возвращает:**
```typescript
interface MessageForDisplay {
  id: string
  threadId: string
  from: { email: string, name: string }
  subject: string
  snippet: string
  date: string
  timestamp: number
  isRead: boolean
  isStarred: boolean
  isImportant: boolean
  aiScore?: number
}
```

### decodeSubject(subject)
Декодирует RFC 2047 encoded subjects (Base64 и Quoted-printable).

### getMessageBody(accessToken, messageId)
Получает полное тело письма в формате raw.

## UI Компоненты

### Поиск
- TextInput с placeholder "Поиск писем..."
- Кнопка ✕ для очистки
- Иконка 🔍 при пустом поиске

### Фильтры
```
[Все] [🔴 Срочные] [Непрочитанные] [🤖 AI-сортировка]
```

- Кнопки переключения фильтров
- Active состояние с синим фоном
- AI-сортировка - зеленая кнопка

### Список писем
- FlatList с RefreshControl
- Бесконечный скролл (onEndReachedThreshold: 0.3)
- Оптимизированное отображение

### Пустые состояния
- Без подключения Gmail → Кнопка "Перейти в настройки"
- Нет писем → "Входящие пусты"
- Нет срочных → "Срочных писем нет"
- Все прочитаны → "Все письма прочитаны"
- Нет результатов поиска → "Попробуйте другой запрос"

## AI Функциональность

### AI-сортировка
Анализирует все письма на основе ключевых слов:

**Срочные ключевые слова:**
- "срочно", "urgent", "asap", "важное", "critical"
- "deadline", "крайний срок", "просрочено"
- → AI Score: 0.95, isImportant: true

**Спам ключевые слова:**
- "unsubscribe", "marketing", "newsletter", "реклама"
- → AI Score: 0.1, isImportant: false

**От известного отправителя:**
- Если имя отправителя > 2 символов
- → AI Score: 0.7, isImportant: true

**Остальное:**
- → AI Score: 0.4-0.5, isImportant: false

## Интеграция с навигацией

### RootStackParamList
```typescript
export type RootStackParamList = {
  BottomTabs: undefined
  Auth: undefined
  GmailAuth: undefined
  MessageDetail: { message: MessageForDisplay }
}
```

### BottomTabNavigator
InboxScreen доступен через вкладку "Inbox" (вкладка 2).

### Навигация
```typescript
// Открыть письмо
navigation.navigate('MessageDetail', { message })

// Вернуться
navigation.goBack()

// На настройки из InboxScreen
navigation.navigate('Settings')
```

## Обработка ошибок

### Gmail не подключен
- Отображается сообщение "Gmail не подключен"
- Кнопка "Перейти в настройки"
- Экран загрузки писем пропускается

### Ошибки при загрузке
- Alert с сообщением об ошибке
- Логируется в console.error
- Можно повторить через pull-to-refresh

## Примеры использования

### Загрузить письма
```typescript
const messages = await gmailService.getMessages(accessToken, 20)
setMessages(messages)
```

### Применить поиск
```typescript
const query = "важное"
applyFilters(messages, filterMode, query)
```

### Применить AI сортировку
```typescript
await handleAISorting()
// Письма переанализированы и отсортированы
```

### Открыть письмо
```typescript
const message = filteredMessages[0]
navigation.navigate('MessageDetail', { message })
```

## Файлы созданные/изменены

### Созданные
- ✅ `src/screens/InboxScreen.tsx` - Главный экран
- ✅ `src/screens/MessageDetailScreen.tsx` - Экран письма
- ✅ `src/components/MessageCard.tsx` - Компонент карточки

### Изменены
- ✅ `src/services/gmailService.ts` - Добавлены методы getMessages, getMessage, decodeSubject, getMessageBody
- ✅ `src/navigation/RootNavigator.tsx` - Добавлен MessageDetail экран
- ✅ `src/types/index.ts` - Добавлены типы MessageForDisplay, GmailMessageFull
- ✅ `src/screens/index.ts` - Экспортированы новые экраны
- ✅ `src/components/index.ts` - Экспортирован MessageCard

## Известные ограничения

### версия 1.0
- Ответ на письма: заглушка (версия 2)
- Полное тело письма: только snippet в превью
- Загрузка вложений: не реализовано
- Отправка писем: не реализовано
- Синхронизация статусов: локальная (не обновляет в Gmail)
- Кэширование писем: нет (каждый раз загружаются заново)

## Performance

### Оптимизация
- Параллельная загрузка писем через Promise.all()
- Мемоизация callbacks через useCallback
- FlatList оптимизация с keyExtractor
- Условный рендер для пустых состояний

### Рекомендации для улучшения
- Добавить кэширование писем в AsyncStorage
- Реализовать infinite scroll пагинацию
- Добавить фон sync для новых писем
- Реализовать более сложный AI анализ

## Версия 2.0 Roadmap

- [ ] Ответ на письма с AI помощью
- [ ] Поддержка вложений
- [ ] Отправка новых писем
- [ ] Черновики (drafts)
- [ ] Архивирование писем
- [ ] Метки (labels)
- [ ] Расширенные фильтры
- [ ] Улучшенный AI анализ тона
- [ ] Синхронизация в реальном времени
- [ ] Оффлайн поддержка
