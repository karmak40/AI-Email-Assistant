## 🎉 Обновление HomeScreen с Gmail интеграцией

### ✨ Основные изменения

#### 1. **Google Profile Service** (`src/services/googleProfileService.ts`)
Новый сервис для получения информации профиля пользователя из Google API:
- `getUserProfile()` - получает полный профиль пользователя
- `getProfilePhoto()` - получает URL фото профиля
- `getUserEmail()` - получает email адрес пользователя

```typescript
import { googleProfileService } from '../services/googleProfileService';

const profile = await googleProfileService.getUserProfile(accessToken);
console.log(profile.email, profile.picture);
```

#### 2. **Обновленный HomeScreen** (`src/screens/HomeScreen.tsx`)
Условный рендеринг в зависимости от статуса аутентификации:

**Для неавторизованного пользователя:**
- Кнопка "Попробовать демо"
- Кнопка "Подключить Gmail"
- Информация о возможностях
- CTA для регистрации

**Для авторизованного пользователя с Gmail:**
- Аватар пользователя (из Google профиля)
- Email пользователя
- Кнопка "Входящие" (переход на экран InboxScreen)
- Кнопка "Написать письмо" (переход на экран ComposeScreen)
- Секция "Быстрые действия":
  - 💬 Ответить на последние (AI анализирует непрочитанные)
  - ⚡ Проверить срочные (AI ищет важные письма)
  - 📋 Саммари за день (AI делает краткое содержание)

#### 3. **Bottom Tab Navigation** (`src/navigation/BottomTabNavigator.tsx`)
Новый компонент для навигации по нижней панели с 4 вкладками:
- 🏠 **Главная** - главный экран
- 📧 **Входящие** - список писем
- ✍️ **Написать** - создание нового письма
- ⚙️ **Настройки** - настройки приложения

#### 4. **Новые экраны**

**InboxScreen** (`src/screens/InboxScreen.tsx`)
- Заглушка для отображения списка писем
- Загрузка писем из Gmail API (TODO)
- Пустое состояние с сообщением

**ComposeScreen** (`src/screens/ComposeScreen.tsx`)
- Форма для написания письма (Кому, Тема, Текст)
- Секция "AI Помощник" с кнопками:
  - ✓ Исправить
  - 🎭 Изменить тон
  - 📝 Расширить текст
- Отправка письма через Gmail API (TODO)

**SettingsScreen** (обновлен)
- Все существующие настройки
- Новая кнопка "Выход из аккаунта" (красная, с подтверждением)

#### 5. **Обновленная навигация**

**RootNavigator** (`src/navigation/RootNavigator.tsx`)
- Условная навигация: Auth или BottomTabs
- Проверка сессии при монтировании
- Подписка на изменения статуса аутентификации
- Loading экран во время инициализации

**Типы навигации** (`src/types/index.ts`):
```typescript
export type RootStackParamList = {
  BottomTabs: undefined;
  Auth: undefined;
  GmailAuth: undefined;
};

export type BottomTabParamList = {
  HomeTab: {
    successMessage?: string;
    gmailEmail?: string;
  } | undefined;
  InboxTab: undefined;
  ComposeTab: undefined;
  SettingsTab: undefined;
};
```

### 📁 Структура файлов

```
src/
├── services/
│   ├── googleProfileService.ts     ✨ НОВЫЙ
│   ├── gmailService.ts
│   ├── supabase.ts
│   └── ...
├── screens/
│   ├── HomeScreen.tsx              🔄 ОБНОВЛЕН
│   ├── InboxScreen.tsx             ✨ НОВЫЙ
│   ├── ComposeScreen.tsx           ✨ НОВЫЙ
│   ├── SettingsScreen.tsx          🔄 ОБНОВЛЕН
│   └── index.ts                    🔄 ОБНОВЛЕН
├── navigation/
│   ├── BottomTabNavigator.tsx      ✨ НОВЫЙ
│   └── RootNavigator.tsx           🔄 ОБНОВЛЕН
└── types/
    └── index.ts                    🔄 ОБНОВЛЕН
```

### 🔄 Поток работы

1. **Запуск приложения**
   - RootNavigator проверяет, авторизован ли пользователь
   - Если нет → показывает AuthScreen
   - Если да → показывает BottomTabNavigator

2. **После авторизации в Gmail**
   - HomeScreen автоматически загружает профиль пользователя
   - Отображает аватар, email и быстрые действия
   - Пользователь может переходить между экранами через Bottom Navigation

3. **Выход из аккаунта**
   - В SettingsScreen нажимаем "Выход из аккаунта"
   - После подтверждения выходим из Supabase сессии
   - Навигация автоматически возвращает на AuthScreen

### 🚀 Что нужно сделать дальше

- [ ] Реализовать загрузку списка писем в InboxScreen
- [ ] Реализовать отправку письма в ComposeScreen
- [ ] Добавить интеграцию с DeepSeek для AI функций
- [ ] Реализовать быстрые действия (Ответить, Проверить срочные, Саммари)
- [ ] Добавить фильтрацию и сортировку писем
- [ ] Тестирование на iOS и Android

### 💡 Примечания

- Все экраны поддерживают светлую и темную тему
- Навигация работает автоматически в зависимости от статуса аутентификации
- Google профиль загружается при каждом открытии HomeScreen
- Токены Google хранятся безопасно в Supabase (защищены RLS)
