# 🔐 Google OAuth 2.0 - Пошаговое руководство

**Как создать OAuth 2.0 Client ID для приложения**

---

## 📋 Предварительные требования

- ✅ Google аккаунт (gmail.com)
- ✅ Проект создан в Google Cloud Console
- ✅ **Gmail API включен** (смотрите ниже)

---

## ⏬ Шаг 0: Включить Gmail API (если еще не включен)

### 0.1 Откройте Google Cloud Console

```
1. Перейдите на https://console.cloud.google.com
2. Если вы не в проекте, откройте выпадающий список вверху
3. Нажмите "SELECT A PROJECT"
4. Выберите ваш проект или создайте новый
```

### 0.2 Включите Gmail API

```
1. В левом меню выберите "APIs & Services" → "Library"
2. В строке поиска найдите "Gmail API"
3. Нажмите на первый результат (Gmail API v1)
4. Нажмите "Enable" (голубая кнопка вверху)
5. Ждите 10-30 секунд пока API включится
6. Должно появиться сообщение "API enabled"
```

✅ **Gmail API теперь включен!**

---

## 🎯 Шаг 1: Создать OAuth 2.0 Client ID

### 1.1 Перейти к credentials

```
1. В левом меню нажмите "APIs & Services" → "Credentials"
   (Или перейдите по ссылке: https://console.cloud.google.com/apis/credentials)
2. Нажмите "CREATE CREDENTIALS" (синяя кнопка вверху)
3. Выберите "OAuth 2.0 Client IDs"
```

### 1.2 Выбрать тип приложения

```
Если вы видите форму "Create OAuth 2.0 Client ID":
1. Нажмите "Configure Consent Screen" (оранжевая ссылка)
2. Смотрите Шаг 2 ниже

Если вы видите опции типа приложения:
1. Выберите "Web application"
2. Перейдите к Шагу 3
```

---

## 📝 Шаг 2: Настроить Consent Screen (если требуется)

### 2.1 Выберите тип пользователя

```
1. Выберите "External" (внешние пользователи)
2. Нажмите "Create"
```

### 2.2 Заполните основную информацию

```
Форма "OAuth consent screen":

1. App name: "AI Email Assistant"
   └─ Можно любое имя вашего приложения

2. User support email: ваш email
   └─ Укажите свой Gmail адрес

3. Нажмите "Save and Continue"
```

### 2.3 Добавьте scopes (разрешения)

```
1. Нажмите "Add or Remove Scopes"
2. В поле поиска введите: "gmail"
3. Выберите "gmail.modify"
   └─ Отмечена checkbox? ✅
4. Нажмите "Update"
5. Нажмите "Save and Continue"
```

### 2.4 Добавьте тестовых пользователей

```
1. Добавьте свой email (в разделе "Test users")
2. Нажмите "Save and Continue"
```

✅ **Consent Screen готов!**

---

## 🔑 Шаг 3: Создать Web Application Client ID

### 3.1 Выберите тип приложения

```
1. Вернитесь на страницу "Credentials"
   Ссылка: https://console.cloud.google.com/apis/credentials

2. Нажмите "CREATE CREDENTIALS" (синяя кнопка)

3. Выберите "OAuth 2.0 Client IDs"

4. В выпадающем меню выберите: "Web application"
```

### 3.2 Введите название

```
Name: "AI Email Assistant - Expo"
└─ Можно любое описательное название
```

### 3.3 Добавьте Authorized redirect URIs

**ВАЖНО!** Это самый критичный шаг:

```
В разделе "Authorized redirect URIs":

1. Нажмите "ADD URI"
2. Введите ТОЧНО: com.aiemailassistant:/oauth2redirect
3. Убедитесь что скопировали правильно (без пробелов)
4. Нажмите "ADD URI" еще раз (обычно требуется 2+ URI)

Примеры дополнительных URI (если нужны):
- http://localhost:8081
- com.aiemailassistant://oauth2redirect
- https://aiemailassistant.com/auth/callback

Но ГЛАВНЫЙ: com.aiemailassistant:/oauth2redirect
```

**Visual Guide:**
```
┌─────────────────────────────────────────┐
│ Authorized redirect URIs                │
├─────────────────────────────────────────┤
│ □ com.aiemailassistant:/oauth2redirect  │ ← ДОБАВЬТЕ ЭТОТ
│ □ http://localhost:8081                 │ ← ОПЦИОНАЛЬНО
│                                         │
│ [+ ADD URI]                             │
└─────────────────────────────────────────┘
```

---

## 💾 Шаг 4: Скопировать Client ID

### 4.1 Скопировать credentials

```
После нажатия "Create", вы увидите модальное окно:

┌──────────────────────────────────────────────┐
│        OAuth 2.0 Client created             │
├──────────────────────────────────────────────┤
│                                              │
│ Client ID:                                   │
│ 123456789-abcdefg.apps.googleusercontent.com│ ← СКОПИРУЙТЕ ЭТО
│                                              │
│ Client Secret:                               │
│ ABCDEFGHijklmnop...                          │ ← СОХРАНИТЕ ТАЙНО
│                                              │
│ [Download JSON]  [OK]                       │
└──────────────────────────────────────────────┘
```

### 4.2 Куда скопировать

```
1. Скопируйте весь Client ID (без пробелов в конце)
2. Откройте файл .env в проекте
3. Добавьте строку:

   EXPO_PUBLIC_GMAIL_CLIENT_ID=YOUR_CLIENT_ID_HERE

4. Замените YOUR_CLIENT_ID_HERE на скопированный ID

Пример:
   EXPO_PUBLIC_GMAIL_CLIENT_ID=123456789-abcdefg.apps.googleusercontent.com
```

### 4.3 Сохранить JSON (рекомендуется)

```
1. Нажмите "Download JSON" (или "Download")
2. Сохраните файл в безопасном месте
3. НИКОМУ НЕ ПОКАЗЫВАЙТЕ этот файл!
4. Это ваши OAuth credentials
```

✅ **Готово! Client ID получен!**

---

## 🔍 Проверка: Всё ли правильно?

### Проверочный список

- [ ] Gmail API **включен** в Google Cloud Console
- [ ] OAuth consent screen **настроен** (тип: External)
- [ ] Web application **создано**
- [ ] Redirect URI добавлена: `com.aiemailassistant:/oauth2redirect`
- [ ] Client ID **скопирован** в .env
- [ ] JSON файл **сохранен** в безопасном месте

---

## 📋 Если что-то пошло не так

### ❌ Ошибка: "The redirect URI is invalid"

**Причина**: Вы добавили неправильный URI

**Решение**:
```
1. Откройте https://console.cloud.google.com/apis/credentials
2. Нажмите на ваш Web application (OAuth 2.0 Client ID)
3. Проверьте "Authorized redirect URIs"
4. Должно быть ТОЧНО: com.aiemailassistant:/oauth2redirect
   └─ Без пробелов, без опечаток, без .com и т.д.
5. Если неправильно, нажмите X и удалите
6. Добавьте правильный URI
7. Сохраните
```

### ❌ Ошибка: "Gmail API is not enabled"

**Причина**: Вы забыли включить Gmail API

**Решение**:
```
1. Перейдите на https://console.cloud.google.com/apis/library
2. Найдите "Gmail API"
3. Нажмите "Enable"
4. Ждите 10-30 секунд
```

### ❌ Ошибка: "Consent screen not configured"

**Причина**: Вы не настроили OAuth consent screen

**Решение**:
```
1. Перейдите на https://console.cloud.google.com/apis/credentials/consent
2. Нажмите "Create"
3. Выберите "External"
4. Заполните форму (смотрите Шаг 2 выше)
```

### ❌ Ошибка: "Cannot find module 'expo-web-browser'"

**Причина**: Не установлена зависимость

**Решение**:
```bash
npm install expo-web-browser
```

---

## 🎯 Примеры Client ID

**Правильный формат:**
```
123456789012-abcdefghijklmnopqrstuvwxyz12345.apps.googleusercontent.com
```

**Где:**
- `123456789012` - ваш Project ID
- `abcdefghijklmnopqrstuvwxyz12345` - Client ID
- `.apps.googleusercontent.com` - домен Google

---

## 🔐 Безопасность

### ⚠️ ВАЖНО

- ✅ **Client ID** - можно делиться, добавлять в .env (EXPO_PUBLIC_)
- ❌ **Client Secret** - НИКОГДА не добавляйте в .env клиентского приложения
- ❌ **JSON файл** - сохраняйте в безопасном месте, не коммитьте в git

### .gitignore

```
Убедитесь что в .gitignore есть:
.env
.env.local
credentials.json
oauth-credentials.json
```

---

## 📞 Быстрая ссылка на Google Cloud Console

```
Credentials: https://console.cloud.google.com/apis/credentials
Library: https://console.cloud.google.com/apis/library
Consent: https://console.cloud.google.com/apis/credentials/consent
```

---

## ✅ Готово!

Теперь у вас есть **Google OAuth 2.0 Client ID** и приложение может:

✅ Авторизовать пользователей через Google  
✅ Получать доступ к Gmail API  
✅ Читать и отправлять письма  

---

## 📚 Дальнейшие шаги

1. **Добавить Client ID в .env**
   ```env
   EXPO_PUBLIC_GMAIL_CLIENT_ID=your_client_id.apps.googleusercontent.com
   ```

2. **Создать таблицы в Supabase** (смотрите SUPABASE_FULL_INIT.md)

3. **Запустить приложение**
   ```bash
   npm start
   ```

4. **Тестировать Gmail интеграцию**
   ```
   - Нажмите "Подключить Gmail"
   - Авторизуйтесь через Google
   - Проверьте что токены сохраняются
   ```

---

**Версия**: 1.0.0  
**Дата**: 19 февраля 2026  
**Статус**: ✅ READY
