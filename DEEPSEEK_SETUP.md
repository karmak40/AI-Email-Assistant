# Настройка DeepSeek API

## Обзор

В приложение добавлена функциональность "Попробовать демо" с интеграцией DeepSeek API для обработки текста.

## Компоненты

### DeepseekService (`src/services/deepseekService.ts`)

Сервис для взаимодействия с DeepSeek API. Предоставляет следующие методы:

#### `polishText(text: string): Promise<string>`
Исправляет грамматические и стилистические ошибки в тексте.

```typescript
const result = await deepseekService.polishText('Привет, как дела?');
```

#### `changeTone(text: string, tone: 'professional' | 'friendly'): Promise<string>`
Меняет тон текста на официальный или дружелюбный.

```typescript
// Официальный тон
const professional = await deepseekService.changeTone(inputText, 'professional');

// Дружелюбный тон
const friendly = await deepseekService.changeTone(inputText, 'friendly');
```

### DemoModal (`src/components/DemoModal.tsx`)

Модальное окно для демонстрации возможностей AI. Включает:

- **Ввод текста**: Поле TextInput для ввода текста письма
- **Три кнопки действий**:
  - "✓ Исправить ошибки" - вызывает `polishText()`
  - "🎩 Официально" - меняет тон на профессиональный
  - "😊 Дружелюбно" - меняет тон на дружелюбный
- **Просмотр результатов**: Отображение оригинального и обработанного текста
- **Повторное использование**: Возможность использовать результат как новый ввод

## Настройка API ключа

### Шаг 1: Получить API ключ

1. Перейти на [https://platform.deepseek.com](https://platform.deepseek.com)
2. Создать аккаунт или авторизоваться
3. В разделе API получить API ключ
4. Убедиться, что у вас есть кредиты для использования API

### Шаг 2: Добавить ключ в .env файл

Создайте файл `.env` в корне проекта (или обновите существующий):

```bash
EXPO_PUBLIC_DEEPSEEK_API_KEY=sk_...your_api_key_here...
```

**Важно**: Переменная должна начинаться с `EXPO_PUBLIC_` для того, чтобы быть доступной в React Native приложении.

### Шаг 3: Перезагрузить приложение

После добавления переменной в `.env`:

```bash
npm start
# Нажать 'r' для перезагрузки приложения
```

## Использование в приложении

### Интеграция в HomeScreen

DemoModal уже интегрирована в HomeScreen:

```tsx
const [demoModalVisible, setDemoModalVisible] = useState(false);

// В JSX:
<Button
  title="Попробовать демо"
  onPress={() => setDemoModalVisible(true)}
  variant="primary"
  size="large"
/>

<DemoModal
  visible={demoModalVisible}
  onClose={() => setDemoModalVisible(false)}
/>
```

## Mock режим

Если API ключ не настроен или недоступен, сервис автоматически использует mock-ответы для демонстрации функциональности.

**Mock режим активируется если:**
- `EXPO_PUBLIC_DEEPSEEK_API_KEY` не установлена
- API ключ пустой

**Пример mock-ответа:**
- Ввод: "Привет, как дела?"
- Исправление: "Привет, как дела? (исправлено грамматикой)."
- Официальный тон: "Уважаемый адресат,\n\nХотел бы обратить Ваше внимание на следующее: Привет, как дела?\n\nС уважением."

## Обработка ошибок

При ошибке запроса к API:
- Пользователю показывается Alert с сообщением "Ошибка" и текстом об ошибке
- Ошибка логируется в консоль
- Состояние загрузки сбрасывается

## Стоимость API запросов

DeepSeek API использует токены для подсчёта стоимости:
- Текст с исправлениями: ~100-200 токенов за запрос
- Смена тона: ~150-300 токенов за запрос

Подробнее о стоимости: [DeepSeek Pricing](https://platform.deepseek.com/pricing)

## Промпты

### Исправление ошибок
```
Исправь грамматические и стилистические ошибки. Сохрани смысл. Верни только исправленный текст.

Текст для исправления:
{userText}
```

### Официальный тон
```
Перепиши в официально-деловом стиле, используйте уважительные обращения и формальные выражения. Верни только переписанный текст без объяснений.

Текст:
{userText}
```

### Дружелюбный тон
```
Перепиши в дружелюбном, неформальном тоне, но сохрани профессионализм и понятность. Используй более casual выражения и позитивные интонации. Верни только переписанный текст без объяснений.

Текст:
{userText}
```

## Тестирование

### Запуск в Expo Go

```bash
npm start
# Отсканировать QR код в Expo Go (iOS/Android)
```

### Тестирование демо-режима

1. Откройте приложение
2. Нажмите на кнопку "Попробовать демо"
3. Введите текст письма
4. Нажмите на одну из кнопок действий
5. Дождитесь результата

## Возможные улучшения

1. **Кэширование результатов** - сохранять обработанные тексты
2. **История запросов** - отслеживать использованные запросы
3. **Пользовательские промпты** - позволить пользователям создавать собственные инструкции
4. **Другие модели DeepSeek** - добавить поддержку deepseek-reasoner
5. **Интеграция с Gmail** - применять обработку к реальным письмам

## Ссылки

- [DeepSeek API Documentation](https://platform.deepseek.com/api-docs)
- [Expo Environment Variables](https://docs.expo.dev/build/variables/)
- [React Native TextInput](https://reactnative.dev/docs/textinput)
