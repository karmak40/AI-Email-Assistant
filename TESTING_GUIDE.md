## 🧪 Инструкция по тестированию обновленного HomeScreen

### Подготовка

1. Убедитесь, что `.env` файл содержит необходимые переменные:
   ```
   EXPO_PUBLIC_SUPABASE_URL=...
   EXPO_PUBLIC_SUPABASE_ANON_KEY=...
   EXPO_PUBLIC_GMAIL_CLIENT_ID=...
   ```

2. Установите зависимости (если нужно):
   ```bash
   npm install
   # или
   yarn install
   ```

### Тестирование сценариев

#### Сценарий 1: Неавторизованный пользователь

1. Запустите приложение: `npm start`
2. Выберите платформу (iOS, Android или Web)
3. **Ожидаемое поведение:**
   - Должен отобразиться `AuthScreen`
   - На экране должны быть кнопки "Вход" и "Регистрация"
   - Нижняя навигация **не должна** отображаться

#### Сценарий 2: Авторизованный пользователь БЕЗ Gmail

1. Авторизуйтесь в приложении
2. Перейдите на главную вкладку (HomeTab)
3. **Ожидаемое поведение:**
   - Должен отобразиться `HomeScreen` для неавторизованного в Gmail пользователя
   - Должны быть видны кнопки:
     - "Попробовать демо"
     - "Подключить Gmail"
   - Должна быть информация о возможностях
   - Должна быть нижняя навигация с 4 вкладками

#### Сценарий 3: Авторизованный пользователь С Gmail

1. Авторизуйтесь в приложении
2. Нажмите "Подключить Gmail"
3. Авторизуйтесь в Google (если требуется)
4. Вернитесь на главную вкладку
5. **Ожидаемое поведение:**
   - Должны быть загружены данные из Google профиля:
     - Аватар пользователя
     - Имя пользователя
     - Email адрес
   - Должны быть видны кнопки:
     - "Входящие" (📧)
     - "Написать письмо" (✍️)
   - Должна быть секция "Быстрые действия" с 3 элементами:
     - 💬 Ответить на последние
     - ⚡ Проверить срочные
     - 📋 Саммари за день
   - Должна быть нижняя навигация

#### Сценарий 4: Нижняя навигация

1. Находясь на HomeScreen, проверьте все вкладки:

   **🏠 Главная (HomeTab)**
   - Должна отображаться главная с профилем или приглашением

   **📧 Входящие (InboxTab)**
   - Должен отображаться пустой экран с сообщением "Нет писем" (заглушка)
   - TODO: Будут загружаться письма из Gmail

   **✍️ Написать (ComposeTab)**
   - Должна отображаться форма с полями:
     - "Кому"
     - "Тема"
     - "Текст письма"
   - Должна быть секция "AI Помощник" с кнопками:
     - ✓ Исправить
     - 🎭 Изменить тон
     - 📝 Расширить

   **⚙️ Настройки (SettingsTab)**
   - Должны быть видны все существующие настройки
   - Должна быть красная кнопка "Выход из аккаунта"

#### Сценарий 5: Переключение темы

1. На любом экране нажмите на кнопку 🌙/☀️ в заголовке
2. **Ожидаемое поведение:**
   - Тема должна переключиться (светлая ↔ темная)
   - Все цвета должны обновиться
   - Кнопка должна изменить иконку

#### Сценарий 6: Выход из аккаунта

1. Перейдите на вкладку "Настройки" (⚙️)
2. Нажмите на красную кнопку "Выход из аккаунта"
3. **Ожидаемое поведение:**
   - Должен появиться диалог с подтверждением
   - Если подтвердить - приложение вернет на `AuthScreen`
   - Если отмена - остаемся на текущем экране

#### Сценарий 7: Логирование

Во время работы проверяйте консоль на ошибки:

```bash
# Запустите приложение с логированием
npm start -- --verbose

# Или подключитесь к device logs:
# Android: adb logcat
# iOS: Xcode Console
```

### Проверка конкретных функций

#### Загрузка Google профиля

```typescript
// В HomeScreen должна вызваться эта функция при открытии:
const loadUserProfile = async () => {
  const { data: { session } } = await supabase.auth.getSession();
  // ...
  const profile = await googleProfileService.getUserProfile(tokenData.access_token);
  setUserProfile(profile);
};
```

**Проверьте в консоли:**
- Нет ошибок при загрузке профиля
- Профиль содержит: `id`, `email`, `name`, `picture`, `given_name`

#### Навигация между экранами

```typescript
// Нажмите на каждую вкладку и проверьте:
- HomeTab: экран с профилем
- InboxTab: заглушка входящих
- ComposeTab: форма письма
- SettingsTab: настройки
```

#### Состояния загрузки

- При первом открытии приложения должен быть `ActivityIndicator`
- При загрузке профиля должен быть `ActivityIndicator`

### Контрольный список

- [ ] Неавторизованный пользователь видит AuthScreen
- [ ] После авторизации видит Bottom Navigation
- [ ] Профиль загружается после Gmail подключения
- [ ] Все 4 вкладки работают
- [ ] Можно переключаться между вкладками
- [ ] Темы переключаются корректно
- [ ] Выход из аккаунта работает
- [ ] Нет ошибок в консоли
- [ ] Все компоненты отображаются корректно на разных экранах

### Возможные проблемы и решения

| Проблема | Решение |
|---------|---------|
| AuthScreen не отображается | Проверьте `EXPO_PUBLIC_SUPABASE_URL` в `.env` |
| Google профиль не загружается | Проверьте `EXPO_PUBLIC_GMAIL_CLIENT_ID` в `.env` |
| Нижняя навигация не видна | Очистите кэш: `npm start -- -c` |
| Ошибки типов TypeScript | Перезагрузите TypeScript: `Ctrl+Shift+P` → "TypeScript: Restart" |
| Белый экран | Проверьте логи в консоли, может быть ошибка в компоненте |

### Следующие шаги

После успешного тестирования:

1. Реализовать загрузку писем в InboxScreen
2. Реализовать отправку письма в ComposeScreen
3. Интегрировать DeepSeek для AI функций
4. Реализовать быстрые действия
5. Добавить unit тесты
6. Опубликовать обновление в App Store/Google Play
