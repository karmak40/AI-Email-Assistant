## 📖 Примеры использования обновленного HomeScreen

### 1. Google Profile Service

```typescript
import { googleProfileService } from '../services/googleProfileService';

// Получить полный профиль пользователя
const profile = await googleProfileService.getUserProfile(accessToken);
console.log('Имя:', profile.given_name);
console.log('Email:', profile.email);
console.log('Фото:', profile.picture);

// Получить только фото
const photoUrl = await googleProfileService.getProfilePhoto(accessToken);

// Получить только email
const email = await googleProfileService.getUserEmail(accessToken);
```

### 2. HomeScreen с условным рендерингом

```typescript
// HomeScreen автоматически:
// 1. Проверяет, авторизован ли пользователь (supabase.auth.getSession())
// 2. Если авторизован - загружает Google профиль
// 3. Отображает либо экран с Gmail данными, либо экран регистрации

// Пользователь получит доступ к:
// - Просмотру аватара и email
// - Кнопке "Входящие" для просмотра писем
// - Кнопке "Написать письмо" для создания письма
// - Быстрым действиям AI
```

### 3. Bottom Tab Navigation

```typescript
// Пользователь может переключаться между вкладками:

// 🏠 HomeTab
// - Главный экран с профилем и быстрыми действиями

// 📧 InboxTab
// - Список входящих писем из Gmail

// ✍️ ComposeTab
// - Форма для написания нового письма
// - AI помощник для редактирования

// ⚙️ SettingsTab
// - Настройки приложения
// - Кнопка выхода из аккаунта
```

### 4. Навигация между экранами

```typescript
// Из HomeScreen перейти на Inbox
navigation.navigate('InboxTab');

// Из любого экрана перейти на Compose
navigation.navigate('ComposeTab');

// Из любого экрана перейти на Settings
navigation.navigate('SettingsTab');

// Выход из аккаунта (автоматическая навигация на Auth)
await supabase.auth.signOut();
```

### 5. Проверка статуса аутентификации

```typescript
// RootNavigator автоматически проверяет статус и показывает нужный экран

// Если пользователь не авторизован:
// - Показывается AuthScreen

// Если пользователь авторизован:
// - Показывается BottomTabNavigator с всеми экранами

// Проверка вручную:
const { data: { session } } = await supabase.auth.getSession();
console.log('Авторизован:', !!session);
```

### 6. SettingsScreen - выход из аккаунта

```typescript
// В SettingsScreen есть кнопка "Выход из аккаунта"

// При нажатии:
// 1. Показывается диалог подтверждения
// 2. После подтверждения вызывается supabase.auth.signOut()
// 3. RootNavigator автоматически показывает AuthScreen

const handleLogout = async () => {
  await supabase.auth.signOut();
  // Навигация автоматически обновится
};
```

### 7. InboxScreen - загрузка писем

```typescript
// TODO: Реализовать загрузку писем из Gmail API

import { gmailService } from '../services/gmailService';

// Получить список писем
const messages = await gmailService.getMessages(accessToken);

// Получить профиль Gmail
const profile = await gmailService.getProfile(accessToken);
```

### 8. ComposeScreen - отправка письма

```typescript
// TODO: Реализовать отправку письма через Gmail API

// В форме пользователь заполняет:
// - Кому (email адрес)
// - Тема письма
// - Текст письма

// AI кнопки помогают:
// - ✓ Исправить (проверка грамматики)
// - 🎭 Изменить тон (формальный/дружеский/профессиональный)
// - 📝 Расширить (добавить деталей)

// После написания письма нажимаем "Отправить"
```

### 9. Быстрые действия (AI)

```typescript
// TODO: Реализовать AI действия через DeepSeek

// На HomeScreen доступны 3 быстрых действия:

// 1️⃣ Ответить на последние
// - AI анализирует последние непрочитанные письма
// - Предлагает готовые ответы

// 2️⃣ Проверить срочные
// - AI ищет письма со словами: срочно, важно, ASAP, urgent и т.д.
// - Выводит их в отдельный список

// 3️⃣ Саммари за день
// - AI делает краткое содержание всех писем за день
// - Выводит ключевые моменты и действия
```

### 10. Темизация

```typescript
// Все компоненты поддерживают светлую и темную тему

// Переключение темы:
// - В HeaderContainer нажимаем на кнопку 🌙/☀️
// - Или в SettingsScreen меняем "Темная тема"

// Все цвета динамически подстраиваются:
const { colors } = useTheme();
console.log(colors.background); // меняется в зависимости от темы
console.log(colors.text);       // меняется в зависимости от темы
```

## 🎯 Типичный сценарий использования

1. Пользователь открывает приложение
2. Если не авторизован → AuthScreen
3. Авторизуется через Supabase
4. Переходит на HomeScreen (главная вкладка)
5. HomeScreen загружает Google профиль
6. Видит аватар, email, быстрые действия
7. Может перейти:
   - На "Входящие" для просмотра писем
   - На "Написать" для создания письма
   - На "Настройки" для выхода
8. В любом месте может нажать на нижнюю навигацию для переключения вкладок

## ✅ Проверка готовности

- ✅ HomeScreen с условным рендерингом
- ✅ Google Profile Service для получения данных профиля
- ✅ Bottom Tab Navigation с 4 вкладками
- ✅ InboxScreen (заглушка)
- ✅ ComposeScreen с формой для письма (заглушка)
- ✅ SettingsScreen с кнопкой выхода
- ✅ RootNavigator с условной навигацией
- ✅ Обновленные типы навигации
- ✅ Нет ошибок компиляции
