# 📚 Руководство по разработке AI Email Assistant

Этот документ содержит подробные инструкции для разработчиков, работающих над проектом.

## 📖 Содержание

1. [Настройка среды разработки](#настройка-среды-разработки)
2. [Структура проекта](#структура-проекта)
3. [Стандарты кодирования](#стандарты-кодирования)
4. [Работа с компонентами](#работа-с-компонентами)
5. [Работа с сервисами](#работа-с-сервисами)
6. [Отладка](#отладка)
7. [Деплой](#деплой)

## Настройка среды разработки

### Требования

- Node.js 16+
- npm или yarn
- Expo CLI: `npm install -g expo-cli`
- Visual Studio Code или другой редактор

### Начало работы

```bash
# Клонируйте репозиторий
git clone <repo-url>
cd AI-Email-Assistant

# Установите зависимости
npm install

# Запустите проект
npm start
```

## Структура проекта

```
AI-Email-Assistant/
├── App.tsx                 # Главная точка входа приложения
├── package.json            # Зависимости проекта
├── tsconfig.json           # Конфигурация TypeScript
├── src/
│   ├── screens/            # Экраны приложения
│   │   ├── HomeScreen.tsx   # Главная страница
│   │   ├── AuthScreen.tsx   # Авторизация
│   │   └── SettingsScreen.tsx # Настройки
│   │
│   ├── components/         # Переиспользуемые компоненты UI
│   │   ├── Button.tsx
│   │   ├── Card.tsx
│   │   ├── HeroSection.tsx
│   │   ├── FeatureItem.tsx
│   │   ├── StepItem.tsx
│   │   ├── DemoModal.tsx
│   │   └── index.ts        # Экспорт всех компонентов
│   │
│   ├── services/           # Бизнес-логика и API интеграции
│   │   ├── api.ts          # AI и Gmail сервисы
│   │   └── supabase.ts     # Supabase интеграция
│   │
│   ├── navigation/         # Навигация приложения
│   │   └── RootNavigator.tsx # Stack и Tab навигация
│   │
│   ├── context/            # React Context
│   │   └── ThemeContext.tsx # Управление темой
│   │
│   ├── types/              # TypeScript типы
│   │   └── index.ts
│   │
│   └── utils/              # Утилиты и хелперы
│       └── themes.ts       # Цветовые схемы
│
├── .env                    # Переменные окружения (не коммитить)
├── .env.example            # Пример переменных окружения
├── README.md               # Основная документация
├── DEVELOPMENT.md          # Этот файл
└── EXAMPLES.md             # Примеры использования
```

## Стандарты кодирования

### TypeScript

- Используйте строгий режим TypeScript
- Определяйте типы для всех параметров функций
- Используйте interfaces вместо type для объектов
- Избегайте `any` типа

```typescript
// ❌ Плохо
function handlePress(data: any) {
  console.log(data);
}

// ✅ Хорошо
interface UserData {
  id: string;
  name: string;
}

function handlePress(data: UserData): void {
  console.log(data);
}
```

### Стили компонентов

- Используйте `StyleSheet.create()` для локальных стилей
- Используйте `useTheme()` hook для доступа к цветам темы
- Следуйте соглашениям об именовании (camelCase)
- Группируйте связанные стили вместе

```typescript
// ✅ Хорошо
const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
  },
  title: {
    fontSize: 20,
    fontWeight: '700',
  },
});
```

### Именование файлов

- Файлы компонентов: `PascalCase.tsx` (напр. `Button.tsx`)
- Файлы сервисов: `camelCase.ts` (напр. `supabase.ts`)
- Файлы типов: `camelCase.ts` (напр. `index.ts`)

## Работа с компонентами

### Создание нового компонента

```typescript
import React from 'react';
import { StyleSheet, View, Text } from 'react-native';
import { useTheme } from '../context/ThemeContext';

interface MyComponentProps {
  title: string;
  onPress?: () => void;
}

export const MyComponent: React.FC<MyComponentProps> = ({
  title,
  onPress,
}) => {
  const { colors } = useTheme();

  return (
    <View style={[styles.container, { backgroundColor: colors.surface }]}>
      <Text style={[styles.title, { color: colors.text }]}>
        {title}
      </Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    padding: 16,
    borderRadius: 8,
  },
  title: {
    fontSize: 16,
    fontWeight: '600',
  },
});
```

### Использование Theme

Все компоненты должны использовать `useTheme()` hook для получения текущего цвета:

```typescript
const { colors, theme, toggleTheme } = useTheme();

// colors - объект с цветами текущей темы
// theme - 'light' или 'dark'
// toggleTheme() - функция для переключения темы
```

### Доступные цвета

- `primary` - основной цвет (синий)
- `accent` - акцентный цвет (такой же синий)
- `background` - цвет фона
- `surface` - цвет поверхности (карточки, панели)
- `text` - основной текст
- `textSecondary` - вторичный текст
- `border` - цвет границ
- `white` - белый цвет
- `dark` - темный цвет

## Работа с сервисами

### Создание нового сервиса

```typescript
// src/services/myService.ts

export const myService = {
  async someFunction(param: string): Promise<string> {
    try {
      // Ваша логика здесь
      return result;
    } catch (error) {
      console.error('Error in someFunction:', error);
      throw error;
    }
  },
};
```

### Использование сервисов в компонентах

```typescript
import { myService } from '../services/myService';

const handlePress = async () => {
  try {
    const result = await myService.someFunction('param');
    console.log(result);
  } catch (error) {
    // Обработка ошибки
  }
};
```

## Отладка

### React DevTools

Используйте React DevTools для инспекции компонентов:

```bash
# Откройте dev меню
# Android: Ctrl + M
# iOS: Cmd + D или Ctrl + D
```

### Console Logging

```typescript
// Используйте console методы
console.log('Info:', data);
console.warn('Warning:', message);
console.error('Error:', error);
```

### Отладка Сети

Используйте встроенный отладчик Expo для проверки сетевых запросов:

```bash
npm start
# Откройте dev меню и выберите "Show network inspector"
```

## Деплой

### Build для производства

```bash
# Android
eas build --platform android --auto-submit

# iOS
eas build --platform ios --auto-submit

# Web
npm run build
```

### Конфигурация EAS

Убедитесь, что у вас есть `eas.json` файл:

```json
{
  "cli": {
    "version": ">= 2.0.0"
  },
  "build": {
    "production": {
      "android": {
        "buildType": "apk"
      }
    }
  }
}
```

## Лучшие практики

### ✅ Делайте

- Используйте TypeScript для всех новых файлов
- Документируйте сложные функции с JSDoc
- Разбивайте большие компоненты на меньшие
- Используйте цвета из `colors` объекта
- Обрабатывайте ошибки в try-catch блоках
- Группируйте связанные компоненты в папки
- Используйте meaningful переменные и функции

### ❌ Не делайте

- Не используйте inline стили в компонентах
- Не забывайте о типизации (избегайте `any`)
- Не используйте хардкодированные значения цветов
- Не создавайте глубокие вложенности компонентов
- Не забывайте про обработку ошибок
- Не коммитьте файлы `.env` с чувствительными данными

## Дополнительные ресурсы

- [React Native Документация](https://reactnative.dev)
- [Expo Документация](https://docs.expo.dev)
- [TypeScript Документация](https://www.typescriptlang.org/docs)
- [React Navigation](https://reactnavigation.org)
- [Supabase Документация](https://supabase.com/docs)

---

Последнее обновление: февраль 2026
